{
  "name": "polymer-test-template",
  "version": "0.0.1",
  "scripts": {
    "start": "polymer install && polymer serve --port 3000"
  },
  "dependencies": {
    "polymer-cli": "1.7.7",
    "bower": "1.8.4"
  }
}